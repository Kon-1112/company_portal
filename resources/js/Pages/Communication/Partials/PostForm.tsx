import React from "react";
import PrimaryButton from "@/Components/PrimaryButton";

export const PostForm: React.FC = (): JSX.Element => {

    return (
        <React.Fragment>
            <PrimaryButton>
                新規作成
            </PrimaryButton>
        </React.Fragment>
    );
}
